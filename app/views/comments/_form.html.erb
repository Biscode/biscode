<%= form_for([@post, @comment]) do |f| %>

  <div class="field">
    <%= f.text_area :body, placeholder: 'Comment ...' %>
  </div>

  <div class="actions">
    <%= f.submit 'Save', class: 'small button round' %>
  </div>
<% end %>
